import 'package:flutter/material.dart';

class AdminPlanning extends StatelessWidget {
  const AdminPlanning({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Planning"),
    );
  }
}
